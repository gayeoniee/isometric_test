# 댕스(Daengs) 프로젝트 — 결정 사항 정리

> 이 문서를 Claude Code 세션 시작 시 붙여넣거나, 프로젝트 루트에 `CONTEXT.md`로 두고
> "CONTEXT.md 읽고 시작해줘"라고 하면 됩니다.

---

## 1. 프로젝트 개요

반려견 케어 플랫폼. AI 에이전트 + 게임(미니룸·산책).

> **2026-08-22 변경 — 비중 재조정 중 (확정 아님)**
> 앱으로 전환되면서 **게임(미니룸·산책)의 비중이 부가 기능 → 거의 메인으로** 올라가는 중.
> 원래는 "AI 에이전트가 주 기능, 미니룸/산책게임은 부가 기능"이었다.
> 아직 정확히 픽스된 건 아니지만, 작업 우선순위를 정할 때 게임을 부가 기능으로
> 취급하지 말 것.

**클라이언트: 안드로이드 네이티브 (Kotlin + Jetpack Compose) — 확정**
백엔드: FastAPI (Python 3.12)

### 역할 분담
- **병원·약국 찾기 지도 — 다른 팀원 담당.** 이 저장소에서 건드리지 않는다.
- 미니룸·홈 화면 — 이 저장소

---

## 2. 기능 구조

### 에이전트 도구 (LangGraph Supervisor가 라우팅)
| 도구 | 구현 |
|---|---|
| 훈련 비서 | **GraphRAG · Neo4j** (원인-증상-훈련법 관계 탐색) |
| 생활 비서 | 일반 RAG · pgvector (사료/산책/계절관리 단순 조회) |
| 병원·약국 찾기 | PostGIS 반경 검색 |
| 사진 분석 | 딥러닝 파인튜닝 → ONNX 추론 (안구/피부/체형) |
| 보행 체크 | **영상 기록용** (판단 안 함, 시점별 비교 목적) |

**프로필 + 진료 이력을 매 요청 컨텍스트로 주입.** 이게 핵심 차별점.
목표 출력 예: "귀 문제로 진료를 3번 보셨어요", "광견병 접종이 12일 남았어요"

### 독립 화면 (에이전트 안 거침)
- 아바타 스튜디오 (사진 → 캐릭터 생성)
- 미니룸 (홈 화면 겸 산책게임 진입점)
- **진료 기록 작성** (자연어 텍스트/음성 → 템플릿 구조화)

> 이유: 의도가 화면 진입 시점에 확정되므로 LLM 라우팅 불필요.
> 단, 여기서 쌓인 기록은 에이전트 컨텍스트로 흘러들어감.

### 진료 기록 작성 (영수증 OCR 대체)
사용자가 자연어로 말하거나 입력 → LLM이 템플릿으로 구조화 → 기록일지로 적립.

```
입력: "오늘 콩이 귀 아파서 병원 갔는데 외이염이래. 약 2주치 받았고 다음주 재검"
   ↓ (음성이면 STT 먼저)
{
  visited_at, chief_complaint, vet_note, medication, follow_up,
  raw_text   # 원문 반드시 함께 보관 (재처리·복구용)
}
```

- **저장 전 사용자 확인 단계 필수** (추출 오류 수정 가능하게)
- `vet_note` = 수의사가 한 말을 옮긴 것. 앱의 판단이 아님 (`diagnosis` 명칭 사용 금지)
- 처리 시간 2~5초 → **동기 처리, 큐 불필요**
- 이 데이터가 에이전트의 "귀 문제로 3번 보셨어요" 응답 근거가 됨

---

## 3. 동기 / 비동기 분리

| 구분 | 대상 | 처리 |
|---|---|---|
| 동기 | 훈련·생활·병원약국 | 요청 안에서 완료 |
| 비동기 | 사진분석·보행영상·아바타생성 | job_id 반환 → Redis 큐 → 워커 |

큐는 `queue:fast`(사진분석) / `queue:slow`(생성) 로 분리.
**개발 초기에는 큐 없이 동기로 만들고, 나중에 워커로 이동.**
→ 단, 처음부터 함수를 분리해두어 나중에 옮기기 쉽게 할 것.

---

## 4. 미니룸 (지금 만들려는 것)

### 확정 사항
- **2D 아이소메트릭.** Unity 사용 안 함 (앱 용량·빌드 복잡도 때문)
- 쿠키런 킹덤 스타일 추구 (그것도 2D 아이소메트릭임)
- 캐릭터는 **스프라이트 시트 프레임 애니메이션**
- 방 크기 **6×6 격자** (모바일 세로 기준)
- 타일 기준 크기 **64×32px** (가로:세로 2:1 — 이 비율이어야 입체로 보임)

### 핵심 구현 규칙 — 좌표 변환 (이 수식 그대로 사용)
```kotlin
// 격자 → 화면
fun toScreen(col: Int, row: Int, tw: Float, th: Float, origin: Offset) = Offset(
    x = origin.x + (col - row) * tw / 2f,
    y = origin.y + (col + row) * th / 2f
)

// 화면 → 격자 (터치 판정)
fun toGrid(pos: Offset, tw: Float, th: Float, origin: Offset): Pair<Int, Int> {
    val dx = pos.x - origin.x
    val dy = pos.y - origin.y
    val col = ((dx / (tw / 2f) + dy / (th / 2f)) / 2f).toInt()
    val row = ((dy / (th / 2f) - dx / (tw / 2f)) / 2f).toInt()
    return col to row
}

// 앞뒤 정렬 — 기준은 col+row (화면 y좌표 아님)
items.sortedBy { it.col + it.row }.forEach { drawItem(it) }
```
정렬을 안 하면 뒤쪽 아이템이 앞쪽을 덮어버림. 가장 흔한 버그.

### 데이터 모델
```kotlin
data class PlacedItem(
    val itemId: String,   // "bed_01"
    val col: Int,         // 0..5 (6x6 격자)
    val row: Int          // 0..5
)
// 화면 좌표는 저장하지 않음 — toScreen()으로 매번 계산
// 앞뒤 순서도 저장하지 않음 — col+row로 결정됨
```

### 구성 요소
| 요소 | 형식 |
|---|---|
| 방 배경 | 아이소메트릭 PNG |
| 가구·아이템 | 투명 PNG, 20~30종 목표 |
| 강아지 아바타 | 사진 → 생성 (힉스필드 검토 중) |
| 사람 아바타 | 파츠 레이어 조합 (룰 기반) |

### 화면 흐름
```
미니룸(홈) → [방문 클릭] → 산책 게임
```

### 산책 게임 방향 (의견 단계, 확정 아님)
- GPS 지도 기반
- **전봇대에 마킹하면 지도에 전봇대가 건설되는** 느낌 — 실제 산책 경로가 지도에 쌓이는 구조
- 아이템 줍기를 넣을지는 미정

### 구현하면서 정정된 것
- 벽 높이 **5칸**, 벽 위 여백 0.5칸 (시안 비율에서 역산)
- 타일 크기는 64×32 고정이 아니라 **화면에 맞춰 스케일**한다. 단 **2:1 비율은 구성상 유지**
  (`tw = 폭/6`, `th = tw/2` 로 계산하므로 비율이 깨질 수 없음)
- 아트는 "기본 단위"(타일 64×32)로 그리고 렌더러가 한 번만 변환한다

### toGrid 주의 — 실제로 물렸던 버그
`toGrid` 의 `.toInt()` 는 floor 가 아니라 **0 방향 잘림**이다.
격자 왼쪽 바깥의 `col = -0.4` 가 `0` 으로 나와서, 밖으로 끌어낸 아이템이
조용히 (0,0) 에 붙는다. 수식은 위 그대로 두고, **범위 검사는 반드시
`toGridF`(잘림 이전 실수값)로** 한다. `IsoMathTest` 가 이걸 고정한다.

---

## 5. 인증

**안드로이드이므로 JWT 방식** (웹용 httpOnly 쿠키 아님)
- 카카오 OAuth 2.0
- 서버 세션은 Redis

---

## 6. 데이터

**PostgreSQL 하나로 통합** (MariaDB 사용 안 함)
- pgvector — 문서 임베딩
- PostGIS — 병원·약국 좌표
- 관계형 — 사용자·반려견·job·진료이력

**Neo4j** — 훈련 지식 그래프 (GraphRAG 전용)
Redis — 세션·캐시·큐
Object Storage — 사진·보행영상·음성파일·생성 이미지

---

## 7. 미결정 사항

- 게임 vs 에이전트 최종 비중 (1번 참고 — 게임 쪽으로 기우는 중)
- 산책 게임 세부 (전봇대 마킹·건설 컨셉을 실제로 갈지)
- 아이템 줍기를 산책 게임에 넣을지
- 강아지 마리 수 (멘토: 5~10 검토). 3마리 / 10마리 눈으로 비교 후 결정
- 방 격자 크기 — 지금 6×6. 늘리면 칸은 늘지만 타일이 작아져서 강아지가 작아진다.
  `RoomSpec.GRID` 한 곳이라 나중에 바꾸기 쉬움. **좁다고 느낄 때 늘릴 것**
- 힉스필드 호출 횟수 — 정지 1장(코드로 걷기) vs 대기+걷기 2벌.
  프로시저럴 걷기를 눈으로 보고 결정
- 여러 칸 차지하는 아이템(2×1 침대 등) 처리 방식
  → `ArtBox.footprint` 필드는 미리 뚫어놨고 현재 전부 1×1
- 아이템 획득 방식 (산책 거리 연동?)
- 멀티 반려견 시 에이전트 컨텍스트 처리 (통합 vs 분리) — 멘토 상담 예정
- Celery vs ARQ
- 훈련 문서 → 그래프 변환 파이프라인 (엔티티/관계 추출 방식)
- 보행 영상을 포즈 추정까지 갈지, 단순 저장만 할지

### 해결된 것
- ~~미니룸 배치 영구 저장~~ → **완료.** SharedPreferences + `RoomCodec`(순수 함수).
  배치·방향·테마가 앱을 완전히 종료해도 남는다. 저장소를 서버/DataStore 로 바꿔도
  형식(`RoomCodec`)은 그대로라 갈아끼우기만 하면 된다
- ~~아트 조달~~ → **직접 제작.** 무료 CC0 3D 킷(KayKit·Kenney·Quaternius)을 받아
  파스텔로 리컬러 후 리렌더. 상세는 `docs/asset-workflow.md`
- ~~픽셀아트 vs 부드러운 벡터~~ → **부드러운 파스텔**(시안 그대로).
  단 `filterQuality` 로 **아이템별로** 고를 수 있게 해둬서 나중에 섞어 써도 된다
- ~~힉스필드 출력 형식~~ → 스프라이트 시트 규격 확정(격자·행 우선, 프레임 크기/열/개수/fps).
  영상으로 나오면 프레임 추출 → 배경 제거 → 격자 합치기가 필요.
  **강아지는 아이소메트릭일 필요 없음** — 정면/3-4 뷰 빌보드 (쿠키런 킹덤 방식)

---

## 8. 주의사항 (반드시 지킬 것)

**사진 분석 출력에 질병명을 절대 넣지 않는다.**
Pydantic 스키마에 질병명 필드 자체를 만들지 않는 방식으로 강제.
```python
class VisionResult(BaseModel):
    observed_features: list[str]   # "좌안 결막 발적"
    severity_band: Literal["관찰됨", "주의 관찰", "빠른 내원 권장"]
    vet_visit_recommended: bool
    disclaimer: str
    # 진단명 필드 없음
```

**보행 체크도 동일 원칙.** 영상은 기록·비교용으로만 제공하고
"이상 있음/없음" 같은 판단 문구를 출력하지 않는다.

---

## 9. 미니룸 — 현재 상태 (2026-08-23 기준)

홈 화면 데모(원래 요청)는 완료. 그 뒤로 미니룸이 계속 확장됐다.
**아래가 지금 코드의 실제 동작이다.**

### 화면
- 시안대로 상단바 / 방 / AI 챗봇 카드 / 산책 요약 / 하단 네비
- **스크롤 없이 한 화면.** 방이 남는 높이를 가져가고, 챗봇 카드와 인벤토리는
  같은 슬롯 높이(`CardSlotHeight`)라 열고 닫아도 방 크기가 안 변한다
- 의존성 추가 **0개** (아이콘·아바타 전부 Canvas)
- `dynamicColor` 끔 — 안드로이드 12+에서 배경화면 색이 시안 팔레트를 덮어버린다

### 아이템 9종
카펫 · 강아지침대 · 침대(사람, **2×1 다칸**) · 책상 · 강아지집 · 밥그릇 · 물그릇 · 화분 · 무드등
- 전부 Canvas 도형. 무드등만 PNG 스프라이트 시트 (파이프라인 검증용으로 남겨둠)
- PNG 교체는 `ItemCatalog.kt` 한 줄. 실기기에서 도형 경로와 동일하게 그려지는 것 확인함

### 조작 — 모드가 갈린다
| | 평소 | 편집 (`＋` 누른 뒤) |
|---|---|---|
| 강아지 | 보임 · 터치로 아무 데나 이동 | **확 숨김** |
| 가구 | 배경 (안 눌림) | 드래그 이동 · 탭하면 `↻ ✕` 버튼 |
| 문 | 눌리면 열림 | 눌리면 열림 |

- 강아지 터치는 **그리는 순서와 무관하게 먼저** 검사한다 → 가구 뒤에 가려져도 잡힌다
- ~~방 밖으로 끌어내면 치우기~~ **삭제됨.** 알려주지 않으면 모르는 조작이었다
- ~~톡 누르면 방향 전환~~ → 탭은 **선택**, 돌리기는 `↻` 버튼

### 강아지 — 격자를 쓰지 않는다
- 위치가 **실수 좌표**(2.7, 3.4). 칸을 안 차지해서 가구와 겹쳐도 되고 여러 마리가 안 부딪힌다
- 알아서 돌아다닌다 (목표 뽑기 → 걷기 → 쉬기 반복)
- **걷는 그림 없이 코드로 걷게 만든다** — 통통 튀기 + 착지 눌림 + 좌우 기울임.
  이게 그럴듯하면 힉스필드 호출이 견종당 2회 → 1회로 준다
- 마리 수는 `RoomDefaults.DOG_COUNT` 하나. 멘토 의견은 5~10마리
- `alwaysOnTop` **뗐음.** 가만히 있을 땐 맞는 규칙이었지만, 돌아다니면 침대 뒤로 갔을 때도
  침대 위에 그려져서 밟고 지나가는 것처럼 보인다. 되돌리려면 ArtBox 에 한 줄
- 위치는 저장 안 한다 (계속 돌아다녀서 의미 없음)

### 방 테마 5종
벚꽃 / 민트 / 라벤더 / 하늘 / 버터. 인벤토리의 `테마` 탭에서 고른다.
벽·바닥·포인트 **세 색만** 정하면 그늘·몰딩·문·울타리는 파생된다.
창밖 하늘·벚꽃은 테마를 안 따른다 (바깥 풍경이라).

### 문 → 산책 게임
- 문을 누르면 열린다 (열림 320ms → 콜백 → 420ms → 닫힘 260ms)
- 활짝 열린 순간 `onDoorOpened` 이 불린다. **산책 게임 생기면 여기서 화면 전환**
- 발바닥이 은은하게 맥동해서 누를 수 있다는 걸 알린다
- 문 규격은 `DoorSpec` 한 곳. 그리기와 터치 판정이 같이 읽는다

### 저장
배치·방향·테마가 앱을 완전히 종료해도 남는다. `RoomCodec`(순수 함수) + SharedPreferences.
화면 회전은 `rememberSaveable`, 앱 재시작은 `RoomStore` 담당.

### 미확인
**강아지 돌아다니기·문 열림을 실기기에서 아직 눈으로 못 봤다** (기기 분리됨).
빌드·테스트는 통과. 특히 **걷는 모션이 그럴듯한지** 확인이 필요하다 — 힉스필드 비용이 걸려 있다.

<details>
<summary>원래 요청 내용 (기록용)</summary>

**홈 화면 데모 (Jetpack Compose) — Android Studio에서 실행 가능하게**

### 목표
첨부한 시안대로 화면이 뜨고, **연결된 안드로이드 폰에서** 눌러볼 수 있는 상태.
**백엔드 연동 없음. 모든 데이터는 하드코딩/로컬 상태.**

### 화면 구성 (위→아래)
1. **상단 바** — 로고, 탭 아이콘(홈/기록/알림/프로필)
2. **아이소메트릭 방** (가장 중요)
   - 벽 2면 + 바닥, 창문·문·러그·가구
   - 중앙에 강아지 (지금은 도형 or placeholder 이미지)
   - 좌상단 오늘 날짜 카드, 우상단 카메라 버튼
   - 하단에 이름표 ("○○이네")
3. **AI 챗봇 카드** — 입력창 + 추천 질문 칩 3개 (동작 안 해도 됨)
4. **오늘의 산책 요약** — 횟수/시간/거리 3분할 + 한마디 카드
5. **하단 네비게이션** — 홈/산책기록/중앙버튼/커뮤니티/마이

### 미니룸 기술 요구사항
- 방 **6×6 격자**, 타일 **64×32px** (2:1)
- 아이템 **드래그 배치 + 격자 스냅**
- **앞뒤 정렬 기준은 `col + row`** (4번 섹션의 좌표 변환 수식 사용)
- 배치는 **격자 좌표(col, row)로 저장**, 화면 좌표는 매번 계산
- 스프라이트 시트 프레임 애니메이션 컴포넌트 포함
- 이미지 에셋 없으므로 **도형(Canvas)으로 대체**, 나중에 PNG 교체 가능한 구조로

### 확인 방법
- `./gradlew assembleDebug` 통과
- Compose Preview로 화면 확인 가능하게 `@Preview` 붙여줄 것
- `./gradlew installDebug`로 **연결된 실기기(폰)** 에서 드래그가 동작할 것
- 상세는 10번 개발 환경 섹션 참고

### 스타일
- 핑크/크림 파스텔 톤 (시안 참고)
- 둥근 모서리, 부드러운 그림자

</details>

---

## 10. 개발 환경 / 실행 방법

### 테스트는 에뮬레이터가 아니라 실기기(안드로이드 폰)로
- 개발자 본인 폰이 안드로이드 → **AVD 에뮬레이터 만들지 않음**
- 드래그 배치 기능은 실제 손가락 터치로 확인해야 감이 정확함
- 다른 화면 크기 검증이 필요할 때만 에뮬레이터 추가

### 실행 흐름
```
Claude Code가 코드 수정
   ↓
Android Studio가 자동 감지
   ↓
./gradlew installDebug  (또는 Android Studio 실행 버튼)
   ↓
연결된 폰에 바로 설치·실행
```

### 팀 규칙 — 파이썬은 uv 로 통일
`pip` 를 쓰지 않는다. 스크립트는 PEP 723 인라인 의존성을 달아두고 `uv run` 으로 실행한다.

```
uv run tools/isoasset.py check 에셋/*.png
```

의존성이 스크립트 안에 적혀 있어서 가상환경을 만들거나 따로 설치할 필요가 없다.
(백엔드 FastAPI 도 같은 규칙을 따를 것)

### 자바 버전 — 따로 설치할 것 없음
Android Studio 에 들어있는 JBR(25) 을 Gradle 데몬이 그대로 쓴다.
`~/.gradle/jdks` 가 비어 있는 게 그 증거. 새 PC 에서도 **Android Studio + Git** 만 깔면 된다.

헷갈리기 쉬운 숫자들:
| 항목 | 값 | 뜻 |
|---|---|---|
| minSdk | 26 | 안드로이드 8.0 이상에서 설치 가능 |
| compileSdk / targetSdk | 37 | 최신 안드로이드에 맞춰 빌드 |
| JDK (Gradle 데몬) | 25 | 빌드를 돌리는 자바 |
| Java 소스 레벨 | 11 | 코드가 컴파일되는 문법 수준 |

### Claude Code가 지켜야 할 것
- 빌드 확인은 `./gradlew assembleDebug`
- 기기 설치는 `./gradlew installDebug`
- **APK를 따로 만들어 옮기는 방식은 사용하지 않음** (팀 배포 시에만 필요)
- Compose **Live Edit**이 동작하도록 프로젝트를 구성할 것
- 모든 주요 Composable에 `@Preview` 추가 (Android Studio 미리보기용)
- 파이썬 도구는 `uv run` 으로 실행 (위 팀 규칙)

### 세션 운용 (참고)
- PC를 켜둔 상태로 VS Code에서 `/remote-control` 실행 → 폰에서 이어서 지시 가능
- 단, **폰에 앱을 설치하려면 PC가 켜져 있고 같은 와이파이여야 함**
- 밖에서는 지시만 가능, 화면 확인은 PC 근처에서
