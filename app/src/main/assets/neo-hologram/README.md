# 네오 채소 도감 (holographic card demo)

강아지 네오가 채소가 된 트레이딩 카드 12장을, 포켓몬 카드 게임 포켓처럼 기울이고
반짝이게 만든 데모입니다.

```
http://localhost:3000/neo-hologram/index.html
```

**`/neo-hologram/` 만 치면 404 입니다.** Next 가 `public/` 안의 디렉터리 인덱스를
잡아 주지 않아서, `index.html` 까지 붙여야 합니다.

## 이 폴더의 전제: 폴더 하나로 자족한다

**빌드 단계가 없습니다.** `public/` 안의 정적 파일이라 그대로 서빙되고, 고치면
새로고침으로 끝납니다. 번들러도 `node_modules` 도 안 씁니다 — 그래서 npm 패키지를
`import` 할 수 없고, 필요한 건 `vendor/` 로 복사해 옵니다.

이 전제를 깨는 변경(빌드 도입, 번들러 도입)은 이 폴더의 성격을 바꾸는 결정이라
`docs/decisions.md` 에 남기고 하세요.

## 파일

| 파일 | 내용 |
| --- | --- |
| `index.html` | 껍데기. **CSS 링크 순서가 중요합니다** (아래 참고) |
| `cards.mjs` | 카드 12장의 데이터. 그림 속 인쇄 문구를 그대로 옮긴 것 |
| `main.js` | 도감 그리드 · 확대 뷰 · 기울기 입력(포인터/키보드) |
| `style.css` | 카드 기본 모양. 프레임 · 그림자 · 자체 포일 · 글레어 |
| `rarity.css` | 레어도별 표면. **cards-css 로 가는 다리**가 여기 있습니다 |
| `immersive.css` / `.mjs` | No.01 전용. 꾹 누르면 카드 안으로 들어가는 별개의 뷰 |
| `vendor/cards-css/` | `@kongyo2/cards-css` 0.5.0 (MIT) 의 포일 CSS. **원본 그대로** |
| `art/` | 카드 그림 (webp). 원본 PNG 는 `tools/art-src/` (gitignore) |

## 카드와 포일

레어도는 등급표가 아니라 **연출 방식**입니다. "위로 갈수록 더 반짝이게" 가 아니라
카드마다 서로 다른 렌더링 기법을 씁니다 — 기법이 겹치면 시험할 게 없어지기 때문입니다.
**12장 전부 다릅니다.**

| No. | 카드 | 포일 | 기법 | 출처 |
| --- | --- | --- | --- | --- |
| 01 | Cabbage | `immersive` | 꾹 누르면 카드 안으로 들어간다 | **우리 것** |
| 02 | Pepper | `prism` | 무지개가 각도로 쪼개지는 분광 | cards-css |
| 03 | Eggplant | `crystal` | 결정 패싯. 면마다 따로 꺾인다 | cards-css |
| 04 | Carrot | `gold` | 금박 (데모의 GOLD SECRET) | cards-css |
| 05 | Danhobak | `oilslick` | 어두운 기름막 무지개 | cards-css |
| 06 | Mushroom | `sunburst` | 중심에서 뻗는 광선 | cards-css |
| 07 | Broccoli | `holo` | 무지개 밴드 + 세로 스캔라인 | cards-css |
| 08 | Cucumber | `reverse` | 가운데를 죽이고 가장자리를 살리는 역전 폴오프 | cards-css |
| 09 | Spinach | `aurora` | 넓고 부드러운 색 띠 | cards-css |
| 10 | Sweet Potato | `cosmos` | 성운 결 | cards-css |
| 11 | Tomato | `mosaic` | 격자로 잘린 타일. 유일하게 무늬가 기하학 | cards-css |
| 12 | Lettuce | `metal` | 세로로 긁힌 브러시드 결. 명도만 오르내리는 이방성 | cards-css |

`cards.mjs` 의 `rarity` 값이 곧 cards-css 의 포일 이름이고, 그대로 DOM 의
`data-effect` 가 됩니다.

### 안 쓰는 포일

cards-css 는 14종입니다. 나머지 셋은 이렇습니다.

| 포일 | 왜 안 쓰나 |
| --- | --- |
| `radiant` | **쓸 수 있는데 안 골랐습니다.** `reverse` 와 느낌이 겹쳐서 11번에서 뺐습니다 |
| `glitter` · `rainbow` | **CSS 만으로는 못 씁니다.** 반짝이 텍스처(`--glitter`)를 원본 JS 가 런타임에 만들어 넣습니다. 우리는 JS 를 안 가져왔습니다 |

`glitter` / `rainbow` 를 살리려면 `--glitter` 에 CSS 로 만든 반짝이를 넣어 주면 됩니다 —
`gold` 의 `--grain` 을 그렇게 처리해 놨으니 그걸 따라 하면 됩니다.

> `mosaic` 은 `--glittersize` 를 쓰는데 **이건 텍스처가 아니라 그냥 길이**입니다.
> 이름이 비슷해서 한 번 잘못 걸렀던 적이 있습니다.

## cards-css 는 어떻게 붙어 있나

원본 파일을 **한 글자도 고치지 않습니다.** 버전을 올릴 때 통째로 갈아끼우려고 그렇게
뒀습니다. 대신 `rarity.css` 의 '다리' 블록이 세 가지를 맞춰 줍니다.

1. **이름** — 우리 `--mouse-x` 를 저쪽 `--pointer-x` 로 넘깁니다
2. **배치** — 저쪽은 grid 로 겹치는 구조인데 우리는 `absolute` 라, `::before`/`::after`
   겹침을 직접 만들어 줍니다
3. **기본값** — `base.css` 에 있던 값 중 우리가 쓰는 포일이 실제로 읽는 것만 옮겨 적었습니다

선택자를 바꾸는 대신 `main.js` 가 우리 요소에 그쪽 클래스명을 얹습니다
(`.stage` → `.holo-card` + `data-effect`, `.foil` → `.holo-card__shine`,
`.glare` → `.holo-card__glare`).

**`base.css`(9.5KB)는 가져오면 안 됩니다.** 포일뿐 아니라 기울기 · 그림자 · 카드
뒤집기까지 들어 있어서 `style.css` 의 `.stage` / `.card` 와 정면으로 부딪힙니다.

**`index.html` 의 링크 순서가 중요합니다.** vendor 링크가 `rarity.css` 보다 **뒤에**
와야 합니다. 특정도가 똑같아서(0,3,0) 순서로만 갈립니다.

## 세기 조정 — 얹으면 얼굴이 날아갑니다

**cards-css 의 기본값이 틀린 게 아니라 원화가 다른 것입니다.** 그쪽 데모 카드는 어두운
벡터 그림입니다. 우리 원화는 정반대로 이미 홀로그램이 인쇄돼 있어 바탕부터 밝고,
**주인공이 흰 강아지**입니다. 포일 대부분이 `color-dodge`(gold 만 `hard-light`)라
밝은 데에 얹으면 흰색으로 클리핑되고, 그러면 제일 먼저 사라지는 게 얼굴입니다.

vendor 파일은 건드리지 말고 `rarity.css` 아래쪽에서 `--hc-*` 로만 누르세요.
`.foil` 의 `opacity` 는 `::before`/`::after` 까지 한 덩어리로 내리므로, 포일 파일 안에
박혀 있는 개별 `opacity` 도 이 한 손잡이에 같이 딸려 옵니다.

| 손잡이 | 언제 |
| --- | --- |
| `--hc-brightness` | 기본. 닷지가 흰색으로 넘어가는 걸 막습니다 |
| `--hc-shine-opacity` | 원화와 섞는 비율. **무늬 안쪽 대비는 안 건드립니다** |
| `--hc-saturate` | 색이 너무 뜰 때 |
| `--hc-glare-opacity` | 하이라이트만 따로 |

**무늬가 구조인 포일은 `brightness` 로 누르면 안 됩니다.** `mosaic` 의 격자와 `metal` 의
세로 결은 무늬끼리의 대비로 생기는 거라, 밝기를 내리면 색이 돌아오는 대신 무늬가 같이
뭉개집니다 — 그러면 그 티어를 고른 이유가 없어집니다. 11번은 `opacity` 로만 누르고,
12번은 `opacity` 를 주로 씁니다.

`hard-light` 포일(`gold` · `metal`)은 `brightness` 를 내리면 blend 가 곱하기 쪽으로
넘어가 탁해집니다. 이 둘도 `opacity` 가 주 손잡이입니다.

지금 눌러 둔 카드는 04 `gold` · 06 `sunburst` · 09 `aurora` · 11 `mosaic` · 12 `metal`
다섯입니다.
**나머지는 일부러 원본 값 그대로입니다** — "얘넨 왜 안 눌렀지" 하고 채워 넣지 마세요.
각 카드의 이유는 `rarity.css` 주석에 날짜와 함께 적혀 있습니다.

**원화를 갈아끼우면 숫자를 다시 봐야 합니다.** 배경이 어두운 원화가 오면 눌러 둔 만큼
심심해집니다.

## 폰에서의 확대 뷰

카드를 누르면 **화면을 가득 채우고 설명은 감춰집니다.** 예전에는 카드(`44svh`)와 설명을
위아래로 나눠 담았는데, 그러면 폰에서 카드가 작아 포일이 거의 안 보였습니다. 이 데모는
카드를 보라고 만든 것이라 화면을 카드에 다 줍니다.

| 제스처 | 동작 |
| --- | --- |
| **문지르기** | **포일 구경** — PC 에서 마우스를 올리는 것과 같습니다 |
| 가운데 탭 | 설명 열기 / 닫기 |
| **가장자리 탭** | 이전 / 다음 카드 — 버튼이 안 보여도 됩니다 |
| 좌우 ‹ › 버튼 | 이전 / 다음 카드 |
| 오른쪽 위 ✕ · 뒤로가기 | 닫기 |

### 쓸어서 넘기기는 없습니다 — 속도로는 못 가릅니다

한동안 "느리면 구경, 빠르면 넘기기"로 속도를 재서 갈랐는데 실기에서 안 됐습니다.
**포일을 구경하다 보면 손이 저절로 빨라집니다.** 카드가 화면 폭을 꽉 채우고 있어서
구경하는 동작 자체가 큰 드래그이고, 어떤 문턱을 잡아도 그 안에 들어옵니다. 문턱을
올리면 이번엔 넘기기가 안 먹습니다.

둘 중 하나는 포기해야 하고, **포일이 이 데모의 본체입니다.** 그래서 카드 안쪽 드래그는
전부 구경에 주고 넘기기는 눈에 보이는 버튼으로 뺐습니다.

되살리고 싶으면 **속도 말고 다른 축**을 찾아야 합니다 — 화면 가장자리에서 시작한
손짓만, 두 손가락, 꾹 누른 뒤에만 같은 것들이요. 속도로는 안 됩니다. 시도했던 값과
결과는 커밋 `9a738f4` 에 남아 있으니 같은 길을 다시 파지 마세요.

### 좌우 버튼은 잠깐만 보입니다

계속 띄워 두면 카드를 가립니다. 확대한 직후와, 카드에서 손을 뗄 때마다 나왔다
사라집니다 (`main.js` 의 `peekNav`).

**버튼이 폰에서 카드를 넘기는 유일한 길이므로 다시 부를 길이 반드시 있어야 합니다.**
그래서 손을 뗄 때마다 다시 나옵니다 — 이게 없으면 한 번 사라진 뒤로 넘길 방법이
없어집니다.

시간은 `NAV_PEEK` 에 둘로 나뉘어 있습니다. `open`(750ms)은 "여기 버튼이 있다"고
알려주기만 하면 되니 짧아도 되지만, `again`(1600ms)은 실제로 눌러야 하는 시간이라
같은 값을 쓰면 다음 장을 연달아 보려 할 때마다 카드를 만졌다 떼야 합니다.

**설명이 열려 있으면 안 나옵니다.** 시트 안에 이전/다음이 이미 있고, 그때 카드 위
드래그는 포일 구경만 합니다.

### 넘겨도 보던 상태를 그대로 들고 갑니다

전체 보기에서 넘기면 전체 보기로, 설명을 펴 둔 채로 넘기면 다음 카드도 설명이 펴진
채로 나옵니다 — 스탯을 비교하며 넘길 때 매번 다시 펴지 않아도 됩니다. 목록에서 새로
열 때는 항상 전체 보기입니다 (`afterClose` 가 접어 둡니다).

### 버튼이 사라져도 그 자리는 살아 있습니다

화면 좌우 `NAV_ZONE`(72px) 안을 **탭**하면 버튼이 안 보여도 넘어갑니다. 버튼이 떠 있는
자리와 대충 겹치므로, 잠깐 보였다 사라지는 그 순간이 "여기가 눌리는 자리"라고 알려주는
역할을 합니다.

**버튼 자체를 계속 눌리게 두는 방식이 아닙니다.** 버튼은 카드 위에 떠 있어서, 그렇게
하면 그 자리에서 포일을 문지를 수 없는 **죽은 띠가 양쪽에 생깁니다.** 여기서 보는 건
'탭'뿐이라 드래그는 카드 어디서든 온전히 구경으로 갑니다.

넓힐수록 넘기기가 쉬워지지만 설명을 여는 가운데가 좁아집니다. 412px 폰에서 72px 이면
가운데가 268px(65%) 남습니다. **설명이 열려 있으면 가장자리 탭도 꺼집니다** — 그때
넘기기는 시트 안의 이전/다음이 맡습니다.

### 뺀 것

- **쓸어서 넘기기** — 위 참고
- **아래로 쓸어내려 닫기** — 포일을 구경하면 세로로도 계속 움직여서 닫기 문턱을 매번
  건드렸습니다. 닫기는 ✕ 와 뒤로가기가 맡습니다
- **배경 탭으로 닫기 (폰에서만)** — 카드가 화면 폭에 맞춰지느라 위아래로 빈 띠가 넓게
  남는데(412×915 폰에서 위아래 185px 씩), 그걸 배경으로 치면 설명을 열려고 탭하다
  빗나갈 때마다 창이 꺼집니다. 폰에서는 그 자리도 탭으로 받습니다. 데스크톱은 그대로입니다

**설명 시트와 버튼 위에서 시작한 손짓은 안 받습니다** — 시트는 스크롤해야 하고 버튼은
눌려야 합니다. 데스크톱에서는 이 코드가 통째로 놀고 있습니다.

## 기울기 입력 — 포인터 · 자이로 · 네이티브

카드를 기울이는 값이 들어오는 문은 셋인데, **전부 `main.js` 의 `writeTilt()` 하나로
모입니다.** 소스를 늘려도 이 함수는 안 건드립니다.

| 소스 | 언제 | 어디에 |
| --- | --- | --- |
| 포인터 (마우스 · 터치 · Shift+화살표) | 항상 | 지금 만지는 카드 한 장 |
| `deviceorientation` (폰 자이로) | secure context 일 때만 | **확대 뷰에 떠 있는 한 장** |
| `window.__neoTilt(beta, gamma)` | 네이티브가 부를 때 | 위와 같음 |

**자이로는 카드를 눌러 확대했을 때만 듭니다.** 그리드에서는 안 듭니다 — 열두 장이
한꺼번에 같은 각도로 도는 게 산만하고, 폰에서 매 프레임 열두 장을 갱신하는 비용도
큽니다. 손가락이 카드에 올라가 있으면 그 프레임은 포인터가 이깁니다.

**PC 는 아무것도 안 바뀝니다.** 데스크톱에는 센서가 없어서 이벤트가 한 번도 안 오고,
포인터 코드가 그대로 돕니다.

세기는 `main.js` 의 `TILT_RANGE`(기본 20도)로 조절합니다 — 키우면 둔해지고 줄이면
예민해집니다. 절대 각도가 아니라 **처음 들어온 자세를 정면으로 삼은 상대값**이라,
폰을 눕혀 보든 세워 보든 들자마자 카드가 홱 돌아가지 않습니다.

### secure context 가 아니면 조용히 안 켜집니다

브라우저는 `deviceorientation` 을 **secure context 에서만** 줍니다. HTTPS 이거나
`localhost` 여야 합니다. **지금 배포(nginx `:80`)는 둘 다 아닙니다** — 폰에서
`daengs.~` 로 들어가면 에러도 경고도 없이 그냥 안 켜집니다. 코드를 의심하기 딱
좋은 형태이니 먼저 주소부터 보세요.

iOS 13+ 는 여기에 더해 `DeviceOrientationEvent.requestPermission()` 을 **사용자
제스처 안에서** 불러야 합니다. 화면을 처음 탭할 때 물어보게 해 뒀습니다. 거절해도
아무 일도 안 일어납니다 — 포인터가 그대로 남습니다.

네이티브 브릿지(`__neoTilt`)는 웹 API 를 안 거치므로 이 제약이 전부 무관합니다.

### 폰에서 확인하기

**둘 다 개발용입니다.** 배포에서 켜려면 nginx 에 TLS 를 붙여야 하고, 그건 되돌리기
번거로운 결정이라 `docs/decisions.md` 감입니다.

**안드로이드 — USB 가 제일 간단합니다.** 인증서가 필요 없습니다.

```powershell
cd frontend; npm run dev          # 평범한 HTTP 로 띄웁니다
adb reverse tcp:3000 tcp:3000     # 폰의 3000 → PC 의 3000
```

폰 크롬에서 `http://localhost:3000/neo-hologram/index.html`. 폰 입장에서 주소가
`localhost` 라 secure context 로 쳐 줍니다. PC 크롬의 `chrome://inspect` 로 그
페이지를 그대로 디버깅할 수 있습니다.

**아이폰 — 로컬 HTTPS 가 필요합니다.**

```powershell
cd frontend; npm run dev:https
```

`frontend/certificates/` 의 인증서로 `https://192.168.0.25:3000` 이 열립니다
(`certificates/` 는 gitignore 입니다). 인증서는 mkcert 가 만든 로컬 CA 로 서명돼
있어서, **그 CA 를 아이폰에 한 번 설치해야** 경고 없이 열립니다.

1. `C:\Users\<사용자>\AppData\Local\mkcert\rootCA.pem` 을 아이폰으로 보냅니다
   (AirDrop · 메일 · 카톡 아무거나)
2. 설정 → **프로파일이 다운로드됨** → 설치
3. 설정 → 일반 → 정보 → **인증서 신뢰 설정** 에서 mkcert 항목을 켭니다
   — **이 3번을 빼먹으면 설치해도 계속 경고가 뜹니다**

**LAN IP 가 바뀌면 인증서를 다시 만들어야 합니다.** 인증서에 IP 가 박혀 있어서
DHCP 로 주소가 바뀌면 안 맞습니다. `next dev --experimental-https` 가 자동 생성하는
인증서는 `localhost` 만 담고 IP 를 안 담으므로, 직접 만들어야 합니다:

```powershell
cd frontend/certificates
$ip = (Get-NetIPAddress -AddressFamily IPv4 | Where-Object { $_.IPAddress -like '192.168.*' } | Select-Object -First 1).IPAddress
& "$env:LOCALAPPDATA\mkcert\mkcert-v1.4.4-windows-amd64.exe" -key-file localhost-key.pem -cert-file localhost.pem localhost 127.0.0.1 ::1 $ip
```

`next.config.ts` 의 `allowedDevOrigins` 도 여기에 걸립니다. Next 는 localhost 가 아닌
곳에서 오는 dev 리소스 요청을 기본으로 막는데, 막히면 앱 페이지가 하얗게 뜹니다
(이 데모는 `public/` 정적 파일이라 막혀도 열리긴 합니다).

### 네이티브 앱에서

WebView 로 띄운다면 값을 직접 밀어 넣는 게 제일 확실합니다 — secure context 도,
권한 대화상자도 없습니다.

```java
webView.evaluateJavascript("window.__neoTilt(" + beta + "," + gamma + ")", null);
```

Capacitor 처럼 `https://localhost` 스킴으로 번들 에셋을 서빙하는 방식이면 브라우저
자이로가 그냥 켜집니다 (스킴은 설정으로 바뀌니 실제로 붙일 때 확인하세요).
원격 URL 을 로드하는 WebView 는 브라우저와 같은 규칙이라 HTTPS 가 필요합니다.

## 카드 한 장 추가하기

1. 원본 PNG 를 `tools/art-src/` 에 두고, **webp 로 변환**해서 `art/` 에 넣습니다.
   높이 1125, 품질 82 가 지금 쓰는 값입니다 (장당 150~250KB). 크롭하지 않습니다 —
   비율이 갈리는 건 괜찮고, 늘이는 게 안 됩니다
2. `cards.mjs` 에 항목을 추가합니다. `w`/`h` 는 **webp 의 실제 픽셀**을 적습니다
   (CSS 가 이 값으로 카드마다 `aspect-ratio` 를 잡습니다)
3. 텍스트는 **그림에 인쇄된 문구를 그대로** 옮깁니다. 안 찍혀 있으면 지어내지 말고
   비웁니다 (No.11 Tomato 는 스탯 라벨이 없어서 비어 있습니다)
4. 포일을 고르고, 그 이름이 `CARDS_CSS_EFFECTS` 에 있는지 확인합니다

## 포일 바꾸기 / 추가하기

**세 군데가 맞아야 화면에 나옵니다.** 하나라도 빠지면 조용히 기본 풀아트로 보입니다.

1. `vendor/cards-css/<이름>.css` 파일이 있을 것
2. `index.html` 에 링크가 있을 것 (rarity.css 뒤에)
3. `cards.mjs` 의 `CARDS_CSS_EFFECTS` 에 이름이 있을 것

새 파일을 vendor 에 넣을 땐 **그 파일이 무엇을 읽는지 먼저 확인하세요.**

```bash
grep -oh 'var(--[a-z0-9-]*' vendor/cards-css/<이름>.css | sort -u
```

다리 블록에 없는 변수를 fallback 없이 읽으면, 그 선언이 **통째로** 죽습니다 (CSS 는
`var()` 가 계산 단계에서 무효가 되면 속성 하나가 아니라 선언 전체를 버립니다).
무늬가 일부만 안 나오는 게 아니라 그 레이어가 통째로 사라지므로, 눈으로만 보면
"원래 이런 효과인가 보다" 하고 넘어가기 쉽습니다.

## 다시 밟지 말 것

- **`--rotate-x` / `--rotate-y` 는 양쪽에 같은 이름으로 있습니다.** 우리 것은 카드를
  실제로 돌리는 각도(deg)이고 저쪽 것은 `.holo-card__rotator` 용입니다. 지금 고른
  포일들은 안 읽지만, vendor 에 파일을 더 넣을 땐 확인하세요. 읽으면 카드가 두 번 돕니다
- **`--tilt-x` / `--tilt-y` 에 우리 `--rotate-x/y` 를 잇지 마세요.** 저쪽은 단위 없는
  맨숫자입니다. deg 를 넣으면 계산이 무효가 되면서 무늬가 통째로 사라집니다
- **마스크와 같은 요소에 `filter` 를 걸면 안 됩니다.** filter 가 mask 보다 먼저
  적용돼서, 오려낸 모양이 아니라 실루엣 전체가 그림자를 집니다. `drop-shadow` 는
  한 겹 바깥 div 에
- **그라디언트 정지점은 커지는 순서여야 합니다.** `r2 < r1` 이면 뒤 값이 `r1` 으로
  클램프돼서 부드러운 페이드가 아니라 하드컷이 됩니다
- **`blur(0px)` 도 공짜가 아닙니다.** 값이 0 이어도 요소마다 필터용 렌더 표면이 생깁니다
- **확대 뷰의 카드에는 `touch-action: none` 이 필요합니다.** 기본값(`.stage` 의
  `manipulation`)이면 손가락을 미는 순간 브라우저가 "스크롤하려나 보다" 하고 제스처를
  가져가서, 페이지로는 `pointercancel` 만 오고 `pointermove` 가 끊깁니다. **그러면
  포일을 문질러 보는 게 통째로 안 됩니다** — 탭만 멀쩡해서 원인을 찾기 어렵습니다.
  그리드의 카드는 세로 스크롤을 해야 하므로 거기서는 끄면 안 됩니다
- **폰에서 `.detail` 의 `transform` 을 애니메이션하지 마세요.** 거기서 설명은
  `translateY(101%)` 로 화면 밖에 숨어 있는 시트라, transform 을 덮어쓰는 순간 숨김이
  풀립니다. `slide()` 의 카드 넘김 연출이 이걸 밟아서 **넘길 때마다 상세가 번쩍했습니다.**
  CSS 애니메이션(`detail-in`)에서 한 번, JS 애니메이션에서 또 한 번 — 같은 함정을 두 번
  밟았으니 세 번째 자리가 없는지 보세요
- **폰에서 데스크톱 연출을 끄는 규칙은, 켜는 규칙보다 뒤에 두세요.** `style.css` 의
  760px 블록은 파일 중간에 있어서, 그 뒤에 정의되는 규칙(여닫는 애니메이션 등)을
  덮지 못합니다 — 특정도가 같으면 순서로만 갈립니다. 실제로 `detail-in` 을 못 껐다가
  **확대할 때 설명 시트가 번쩍 나타났다 사라지는** 증상이 났습니다. 끄는 규칙은
  켜는 규칙 바로 아래에 별도 미디어 블록으로 둡니다
- **`e.target === dialog` 로 배경을 판정하지 마세요.** 폰에서 `.viewer-inner` 가 화면을
  다 덮게 되면서 그 조건이 영영 참이 되지 않습니다. "카드도 설명도 버튼도 아닌 곳"으로
  봐야 합니다
- **`will-change` 를 카드 전부에 걸지 마세요.** 활성 카드에만 겁니다 — 12장에 걸면
  레이어가 12개 생깁니다

## 아직 안 닫힌 것

- **이머시브 진입 순간 600KB 레이스.** 에셋에 선로딩이 없어서 꾹 누른 그 순간 처음
  받습니다. 느린 망에서는 틀만 녹고 빈 텃밭이 뜹니다. 꾹 누르는 520ms 를 선로딩에
  쓰는 게 자연스러운 자리입니다
- **겉잎이 아직 진짜 레이어가 아닙니다.** 속과 같은 픽셀을 고리로 오려 쓰고 있어서,
  비켜도 새로 드러나는 게 없습니다. 겹마다 그린 원화가 오면 `.dio-rind` 의 마스크만
  빼고 `scene.shells` 에 `src` 를 넣으면 됩니다
- **자이로가 배포에서는 안 켜집니다.** nginx 가 `:80` 이라 secure context 가 아닙니다.
  코드는 들어가 있으니 TLS 만 붙으면 켜집니다 (위 '기울기 입력' 참고)
- **이머시브 뷰에는 자이로가 안 붙어 있습니다.** 확대 뷰까지만 붙였습니다

## 라이선스

`vendor/cards-css/` 는 [@kongyo2/cards-css](https://github.com/kongyo2/cards-css)
0.5.0, MIT. 같은 폴더의 `LICENSE` 를 함께 둡니다. 나머지는 이 저장소의 라이선스를
따릅니다.
